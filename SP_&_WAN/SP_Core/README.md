#SP Core and Enterprise WAN Edge
-Performed manual configuration of VTY SSH access, IP addressing, and initial routing (OSPF).
-Migrated OSPF to IS-IS for improved scalability and SP core compatibility.
-Conducted soft OSPF decommissioning by increasing administrative distance (AD) to 150.
-Implemented MPLS Traffic Engineering (TE) for optimized path utilization.
-Configured VRFs at Provider's Edge for traffic segmentation.
-Deployed Multiprotocol BGP (MP-BGP) for scalable inter-domain routing.
-Completed full OSPF decommissioning.

#Key Features / Enhancements:
-Automated pre-migration backups to ensure configuration safety.
-Multithreaded execution for efficient large-scale lab deployment.
-Git-based configuration tracking for version control and auditability.
